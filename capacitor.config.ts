import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dude-where-is-my-car.airam.com',
  appName: "Dude, Where's My Car?",
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
